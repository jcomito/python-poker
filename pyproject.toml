[tool.poetry]
name = "python-poker"
version = "0.1.0"
description = ""
authors = ["Jason Comito <jdcomito@protonmail.com>"]
readme = "README.md"
packages = [
    { include = "poker" }
]

[tool.poetry.dependencies]
python = "^3.13"
flask = "^3.1.3"

[tool.poetry.group.dev.dependencies]
black = "^25.1.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
